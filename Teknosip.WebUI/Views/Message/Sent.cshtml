@using Teknosip.WebUI.Services
@using Teknosip.Application.Features.Messages.Models
@model PaginatedSentMessagesDto

@{
    ViewData["Title"] = "Giden Kutusu";
    Layout = ViewBag.LayoutPath ?? "~/Views/Shared/_Layout.cshtml";
}

<h4 class="fw-bold py-3 mb-4">
    <span class="text-muted fw-light">İletişim /</span> Mesajlar
</h4>

<div class="card shadow-sm border-0">
    <div class="card-header d-flex justify-content-between align-items-center bg-white border-bottom pb-3">
        <h5 class="mb-0 fw-semibold text-dark"><i class="bx bx-send text-primary me-2"></i>Giden Mesajlar</h5>
        <a asp-action="Send" class="btn btn-sm btn-primary">
            <i class="bx bx-edit-alt me-1"></i> Yeni Mesaj
        </a>
    </div>

    <div class="card-body p-0">
        <div class="list-group list-group-flush">

            @if (Model.Messages != null && Model.Messages.Any())
            {
                @foreach (var item in Model.Messages)
                {
                    string avatarUrl = string.IsNullOrEmpty(item.ReceiverImageUrl)
                                       ? "/images/profiles/default.webp"
                                       : item.ReceiverImageUrl;

                    <div class="list-group-item list-group-item-action d-flex align-items-center p-3 bg-white" style="border-bottom: 1px solid #f0f2f4; transition: all 0.2s ease-in-out;">

                        <div class="flex-shrink-0 me-3">
                            <img src="@avatarUrl" alt="@item.ReceiverName" class="rounded-circle shadow-sm" style="width: 48px; height: 48px; object-fit: cover;">
                        </div>

                        <div class="flex-grow-1 overflow-hidden pe-3">
                            <h6 class="mb-1 fw-bold text-truncate" style="color: #435971; font-size: 0.95rem;">@item.ReceiverName</h6>
                            <p class="mb-0 text-truncate" style="font-size: 0.875rem; color: #697a8d;">
                                @item.MessageContent
                            </p>
                        </div>

                        <div class="flex-shrink-0 d-flex align-items-center gap-3">

                            <small class="text-muted text-nowrap">
                                <i class="bx bx-time-five me-1"></i>@DateTimeExtensions.ToTimeAgo(item.CreatedAt)
                            </small>

                        </div>
                    </div>
                }
            }
            else
            {
                <div class="text-center p-5">
                    <div class="d-inline-flex align-items-center justify-content-center rounded-circle mb-3" style="width: 80px; height: 80px; background-color: rgba(105,108,255,.08);">
                        <i class="bx bx-paper-plane" style="font-size: 2.5rem; color:#696cff;"></i>
                    </div>
                    <h5 class="fw-semibold text-dark mb-1">Giden kutunuz boş</h5>
                    <p class="text-muted mb-0">Henüz hiç mesaj göndermediniz.</p>
                </div>
            }

        </div>
    </div>
</div>

@if (Model.TotalPages > 1)
{
    <div class="card-footer d-flex justify-content-center bg-white border-top pb-0 pt-4">
        <nav aria-label="Page navigation">
            <ul class="pagination pagination-sm">

                <li class="page-item @(!Model.HasPreviousPage ? "disabled" : "")">
                    <a class="page-link" asp-action="Sent" asp-route-page="@(Model.CurrentPage - 1)">
                        <i class="tf-icon bx bx-chevrons-left"></i>
                    </a>
                </li>

                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                        <a class="page-link" asp-action="Sent" asp-route-page="@i">@i</a>
                    </li>
                }

                <li class="page-item @(!Model.HasNextPage ? "disabled" : "")">
                    <a class="page-link" asp-action="Sent" asp-route-page="@(Model.CurrentPage + 1)">
                        <i class="tf-icon bx bx-chevrons-right"></i>
                    </a>
                </li>

            </ul>
        </nav>
    </div>
}